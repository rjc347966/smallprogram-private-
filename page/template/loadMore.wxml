<template name="loadMore">
    <view style="width:100%;height:100rpx;line-height:100rpx;font-size:28rpx;color:#b2b2b2;text-align:center">
        <block wx:if="{{hasMore}}">
            正在加载...
        </block>
        <block wx:else>
            已经到底了。
        </block>
    </view>
</template>